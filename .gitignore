node_modules
dist
.lhx-kit
.DS_Store
*.log
.env.local
.env.*.local

# Editor
.vscode/*
!.vscode/extensions.json
.idea

# Test artifacts
coverage
playwright-report
test-results

# MSW service worker is generated by `npx msw init`
public/mockServiceWorker.js
**/dist
.env

# playground docs sites (playgrounds/*) — entirely generated at `pnpm sync`/`pnpm dev`/
# `pnpm build` time by syncing spec-hub.config.yaml's registered repos (homepage included, see
# design.md Decision 8), so nothing under docs/ is committed.
doc_build
**/.vitepress/cache
playgrounds/rspress/docs/*
playgrounds/vitepress/docs/*
!playgrounds/rspress/docs/.gitkeep
